{
  "name": "demo-node-http-app",
  "version": "1.0.0",
  "description": "Lightweight demo Node.js app with zero runtime dependencies and built-in unit/integration tests.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "test:watch": "node --test --watch"
  }
}
